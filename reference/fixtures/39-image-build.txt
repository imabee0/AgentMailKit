Fixture 39 — Docker image build, 2026-08-19, workstation

VERDICT: built

WHY THIS FIXTURE EXISTS
The previous revision recorded VERDICT: the image was NOT BUILT, blocked by Claude sandbox
egress (production.cloudfront.docker.com:443). That was true in the sandbox and is not
true here. `scripts/plan-ledger.sh`'s `ci-image-built` check greps this file for
`^VERDICT: built`.

== environment ==
$ docker version
 Client 29.6.2   Server 29.6.2   Storage Driver: overlayfs
$ df -h /
 /dev/mapper/cryptroot  464G  306G  133G
$ rustc --version
 rustc 1.94.1 (e408947bf 2026-03-25)

== what was fixed BEFORE the first build, so the pipe was not sent broken ==

1. Base images pulled by digest first (not during `cargo build`):
   rust:1.94.1-slim-bookworm@sha256:cf9dd0ec73e75f827fe59123fff9dc65af1a1c8363c3c31ee8d7f8ad0b6a5fb2
   debian:bookworm-slim@sha256:abd67ffcfa541b485a3dff59865ab629aa048a6c613e639d36e7456b0b229241
   cargo-chef 0.1.78 present on crates.io (cksum ada79886…e2ef76fc).

2. No `.dockerignore`. Context was 8.9G (`target/` + venvs). Added `.dockerignore` so
   `COPY . .` is 3.31MB, not a 9G transfer that also invalidates chef on every local compile.

3. `DOCKER_BUILDKIT=1 docker build` failed immediately: "BuildKit is enabled but the buildx
   component is missing". Engine 29 has BuildKit; the CLI plugin was absent
   (`/usr/lib/docker/cli-plugins` had only docker-compose). Installed user-local
   `~/.docker/cli-plugins/docker-buildx` v0.36.1. Did not retry the same command until that
   existed. Did not disable BuildKit: the Dockerfile uses `--mount=type=cache`.

== build ==
$ SOURCE_DATE_EPOCH=1787172494 docker build --progress=plain -t amk:local-validate .
 start=2026-08-19T22:32:38Z  elapsed_sec=100  end=2026-08-19T22:34:18Z
 context transferred: 3.31MB
 chef cook: compiled the 308-dep graph (~40s)
 cargo build --release -p amk-cli --bins: 14.54s (workspace only)
 id=sha256:9afadd312bbdf556865145316d15d692d1ad280c84d1c5891429ced25594c076
 size_bytes=41564593
 created=2026-08-19T20:48:14Z   # SOURCE_DATE_EPOCH from commit, not the clock

== properties, each a command ==
$ docker run --rm --entrypoint id amk:local-validate
 uid=10001(amk) gid=10001(amk) groups=10001(amk)

$ docker run --rm amk:local-validate --role api
 AMK_DATABASE_URL is not set. This must be the Postgres connection string to use for this
 deployment; there is no default, so a missing value never silently falls back to a
 development database.
 exit=1

$ docker run --rm --entrypoint amk amk:local-validate --help
 COMMANDS: init, migrate, doctor

$ docker run --rm --entrypoint amkd amk:local-validate --help
 ROLES: api, smtpd, worker (not implemented — amk-jobs), all (not implemented)

$ docker run --rm --user 0 --entrypoint ls amk:local-validate /etc/ssl/certs/ca-certificates.crt
 /etc/ssl/certs/ca-certificates.crt

== cargo-chef, both directions ==
Source-only (appended `// chef-cache-probe` to crates/amk-http/src/lib.rs, then reverted):
  #14 RUN cargo chef cook --release --recipe-path recipe.json
  #14 CACHED
  #16 RUN cargo build --release -p amk-cli --bins
  Finished `release` in 14.12s
  elapsed_sec=20

Cargo.lock trailing newline (content change, recipe.json identical):
  #14 CACHED
  elapsed_sec=20
  Touching the lockfile is NOT enough if the dependency graph is unchanged. The cook
  layer is keyed on recipe.json, which is the graph, not the lockfile's bytes. A real
  dep bump would invalidate cook; a blank line must not.

== container against host Postgres (127.0.0.1:55432, docker amk-dev-postgres) ==
First attempt failed before listen:
  AMK_DKIM_KEYS is set but unusable: cannot read /keys: Permission denied (os error 13)
  uid 10001 vs a 0700 host mount. Fail-closed, not an image defect. chmod 755/644 on the
  key dir and retry the RUN, not the BUILD.

$ docker run --network host -v $KEYS:/keys:ro -e AMK_DATABASE_URL=... -e AMK_DKIM_KEYS=/keys \
    -e AMK_BIND=127.0.0.1:8198 amk:local-validate --role api
 health=200 body=ok
 ready=ready code=200
 log: "DKIM keyring loaded" keys=s20260819._domainkey.smoke.test count=1
 11 of 11 migrations applied via `amk migrate` in the same image.

== not claimed ==
GHA `cache-from: type=gha` is untested here (no Actions run). `ci.yml`'s docker job still
only runs on push to main. `hadolint` was not re-run; fixture 39 previous revision already
recorded CLEAN at failure-threshold=warning.
