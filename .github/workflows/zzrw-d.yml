name: zzrw-d
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

permissions:
  contents: read
jobs:
  deploy:
    name: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
