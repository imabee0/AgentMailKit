name: zzrw-c
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

permissions:
  contents: read
jobs:
  deploy:
    environment:
      name: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
